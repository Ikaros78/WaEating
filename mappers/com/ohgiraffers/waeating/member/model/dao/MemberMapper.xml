<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ohgiraffers.waeating.member.model.dao.MemberMapper">
	<resultMap type="com.ohgiraffers.waeating.member.model.dto.MemberDTO" id="memberResultMap">
		<id property="id" column="MEMBER_ID"/>
		<result property="pwd" column="MEMBER_PWD"/>
		<result property="email" column="EMAIL"/>
		<result property="name" column="NAME"/>
		<result property="memberType" column="MEMBER_TYPE"/>
		<result property="phone" column="PHONE"/>
		<result property="emailCheck" column="EMAIL_CHECK"/>
	</resultMap>
	
	<select id="selectEncryptedPwd" resultType="java.lang.String">
		SELECT
				A.MEMBER_PWD
		  FROM MEMBER_INFO A
		 WHERE A.MEMBER_TYPE = #{ memberType }
		   AND A.MEMBER_ID = #{ id }
	</select>
	
	<select id="selectLoginAdmin" resultMap="memberResultMap">
		SELECT
				A.MEMBER_ID
			  , A.MEMBER_PWD
			  , A.EMAIL
			  , A.NAME
			  , A.MEMBER_TYPE
			  , A.PHONE
			  , A.EMAIL_CHECK	
		  FROM MEMBER_INFO A
		 WHERE A.MEMBER_ID = #{ id }
	</select>
</mapper>